import React from 'react';
import Heading from '~/Heading';

/**
 * About component
 */
const About = () => (
  <section>
    <Heading level={2}>About</Heading>
    <p>An up-to-date React starter kit with preconfigured Babel and Webpack.</p>
  </section>
);

export default About;
